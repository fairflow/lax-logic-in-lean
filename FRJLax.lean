/-
FRJ◯ — the root module.

FRJ(G) (Fiorentini–Ferrari, ACM TOCL 21(3), Article 22, 2020) extended
with the lax modality ◯, built effective and choice-free from line one.
The plan is `docs/frj-lax-plan.md`; the fidelity table is
`docs/frjlax-fidelity.md`.

Nothing here imports `FRJ/`, `FRJO/`, `Reject/` or `BiLax/`.
-/
import FRJLax.Core
import FRJLax.Model
import FRJLax.Modal
import FRJLax.Calculus
import FRJLax.Paper
import FRJLax.Circ
