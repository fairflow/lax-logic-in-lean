import frontier

/-! # Frontier sampler — inspection pass (no search)

What the fifteen strata generate, what the cells weigh, and whether the
target table READS the budget (`act`).  Durable output:
`wip/frontier_profile.txt`. -/

open PLLND.Frontier

#eval showAll 6
#eval actProfile
