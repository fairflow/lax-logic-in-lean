/-
FORWARDING STUB (2026-08-25).  The V-consequence theorems were hoisted to
`Certified/RhoFRJV.lean` (namespace unchanged: `FRJVConsequences`) so that
wip-free closures (`RNDB.DB`) can cite them; the witness derivations went
to `FRJ/WitnessV.lean` and `FRJ/WitnessV1215.lean`.  This stub keeps
existing `import wip.frjv_consequences` lines compiling.
-/
import Certified.RhoFRJV
