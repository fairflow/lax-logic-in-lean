

B Function: test
  Categories:
  1 foo {this is the first category}
    * bar [single]
    * gump [if not(free)]
  2 bar {this is the second category}
    * foo [property free]
    * bar [property free]
EndFunction;
